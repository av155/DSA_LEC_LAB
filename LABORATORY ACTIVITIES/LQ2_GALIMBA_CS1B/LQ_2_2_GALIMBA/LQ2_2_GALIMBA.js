// Prompt for dog breed
let breed = prompt("Enter your dog breed:");
// Prompt for print count
let count = parseInt(prompt("Enter your desired print count:"));

// Loop to print dog breed
for (let i = 1; i <= count; i++) {
    console.log(breed);
}